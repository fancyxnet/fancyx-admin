import httpClient from '@/utils/httpClient.ts';
import type { AppResponse, PagedResult, PageSearch } from '@/types/api';

/**
 * 新增${function_name}
 * @param req
 */
export function add${business_name}(req: ${class_name}) {
  return httpClient.post<${business_name}, AppResponse<boolean>>('/${module_name_api_prefix}-api/${business_name}/Add', req);
}

/**
 * ${function_name}分页列表
 * @param req
 */
export function get${business_name}List(req: ${business_name}QueryDto) {
  return httpClient.get<${business_name}QueryDto, AppResponse<PagedResult<${business_name}>>>('/${module_name_api_prefix}-api/${business_name}/List', { params: req });
}

/**
 * ${function_name}查询
 * @param req
 */
export function get${business_name}(id: string) {
  return httpClient.get<string, AppResponse<${class_name}>>('/${module_name_api_prefix}-api/${business_name}/Get${business_name}/' + id);
}

/**
 * 修改${function_name}
 * @param req
 */
export function update${business_name}(req: ${class_name}) {
  return httpClient.put<${business_name}, AppResponse<boolean>>('/${module_name_api_prefix}-api/${business_name}/Update', req);
}

/**
 * 删除${function_name}
 * @param id
 */
export function delete${business_name}(id: string) {
  return httpClient.delete<string, AppResponse<boolean>>('/${module_name_api_prefix}-api/${business_name}/Delete/'+id);
}

export interface ${class_name} {
${ts_interface_fields}
}

export interface ${business_name}QueryDto extends PageSearch {
${ts_interface_query_fields}
}
